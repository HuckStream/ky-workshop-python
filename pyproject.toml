[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool]
[tool.poetry]
package-mode = false
[tool.poetry.dependencies]
pulumi = ">=3.197.0,<4.0.0"
pulumi-aws = ">=7.7.0,<8.0.0"
pulumi-awsx = ">=3.0.0,<4.0.0"
pulumi-random = ">=4.18.3,<5.0.0"
python = "^3.9"
